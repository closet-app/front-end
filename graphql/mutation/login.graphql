mutation Login($options: LoginUserInput!) {
  login(options: $options) {
    errors {
      field
      message
    }
    user {
      email
      firstName
      lastName
      id
    }
    token
  }
}
